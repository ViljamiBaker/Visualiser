package visualiser.Renderer.Util;

public enum RendererUpdateType {
    //updates all nodes
    UpdateAll,
    // updates n nodes
    UpdateNumber,
    // updates nodes until time is over some max
    UpdateToTime,
}
